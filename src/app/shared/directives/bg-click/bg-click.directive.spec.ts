import { BgClickDirective } from './bg-click.directive';

describe('BgClickDirective', () => {
  it('should create an instance', () => {
    const directive = new BgClickDirective();
    expect(directive).toBeTruthy();
  });
});
